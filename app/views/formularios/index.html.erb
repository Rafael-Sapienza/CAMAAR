<main class="app-page template-page">
  <h1>Formulários</h1>

  <% if notice %>
    <p class="app-alert app-alert--success"><%= notice %></p>
  <% end %>

  <% if alert %>
    <p class="app-alert app-alert--error"><%= alert %></p>
  <% end %>

  <section id="meus-formularios" class="template-section">
    <h2>Meus formulários</h2>

    <% if @user_formularios.any? %>
      <div class="template-form__cards">
        <% @user_formularios.each do |formulario| %>
          <%= render "card", formulario: formulario %>
        <% end %>
        <%= link_to new_formulario_path, class: "template-form__add-template-button", title: "Criar novo formulário", aria: { label: "Criar novo formulário" } do %>
          <%= image_tag "icons/plus.svg", alt: "", width: 24, height: 24 %>
        <% end %>
      </div>
    <% else %>
      <div class="template-form__cards">
        <p class="app-empty-state template-form__empty-state">Nenhum formulário criado para este semestre</p>
        <%= link_to new_formulario_path, class: "template-form__add-template-button", title: "Criar novo formulário", aria: { label: "Criar novo formulário" } do %>
          <%= image_tag "icons/plus.svg", alt: "", width: 24, height: 24 %>
        <% end %>
      </div>
    <% end %>
  </section>

  <section class="template-section">
    <h2>Outros formulários</h2>
    <% if @other_formularios.any? %>
      <div class="template-form__cards">
        <% @other_formularios.each do |formulario| %>
          <%= render "card", formulario: formulario %>
        <% end %>
      </div>
    <% else %>
      <p class="app-empty-state template-form__empty-state">Nenhum formulário de outros administradores encontrado</p>
    <% end %>
  </section>

</main>
